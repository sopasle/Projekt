Config:
git config --global user.name "USERNAME"
git config --global user.email "EMAIL ADRESSE"

Clonen:
git clone https://github.com/sopasle/Projekt.git

Commiten:
$ git add .
$ git commit -m "USERNAME"
$ git push origin master

Pullen:

$git pull origin master
