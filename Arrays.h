#include <sdsl/int_vector.hpp>
using namespace sdsl;
class Arrays
{
  public:                              // öffentlich
	//Arrays();                      // der Default-Konstruktor
	//Arrays(const Arrays& a);   // Copy-Konstruktor
	//~Arrays();                     // der Destruktor
 	int_vector<> is;
	int_vector<> ie;
	int_vector<> d;
  //private:                             // privat


};
